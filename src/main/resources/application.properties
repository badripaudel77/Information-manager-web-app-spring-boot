#config file
server.port = 8080

#lets use postgres
#here machine ip can be found from ipconfig command under ipv4 address. I am doing this because I am running postgres on my local machine , so docker doesn't know about postgress
# because it treats localhost now to the docker container and not physical machine. We've to tell that hey docker postgres is on this IP which is my computer. 
spring.datasource.url=jdbc:postgresql://localhost:5432/info_keeper_sb
spring.datasource.username=postgres
spring.datasource.password=_BadriPostgres1@

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL81Dialect

spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = update

logging.level.org.springframework.web: DEBUG
logging.level.org.hibernate: ERROR

spring.devtools.restart.enabled=true

#enable file uploading
multipart.enabled=true 
#limit the file size to 10MB
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB


#properties to send the mail using spring boot

# TLS , port 587
spring.mail.properties.mail.smtp.starttls.enable=true

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=email 
spring.mail.password=password


# Other properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
